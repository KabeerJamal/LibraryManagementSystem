
<div class="reservation-limit">
    <h2>Current Reservation Limits</h2>
    <p>Books: <span id="current-book-limit"><%= bookLimit %></span></p>
    <p>Copies: <span id="current-copy-limit"><%= copyLimit %></span></p>
    <p>Reservation Limit a day: <span id="current-reservation-limit-day"><%= reservationLimitDay %></span></p>
</div>

<div class="update-reservation-limit">
    <h2>Update Reservation Limits</h2>
    <form action="/updateReservationLimit" method="POST">
        <label for="book-limit">Books:</label>
        <input type="number" id="book-limit" name="bookLimit" min="0" value="<%= bookLimit %>">
        <br>
        <label for="copy-limit">Copies:</label>
        <input type="number" id="copy-limit" name="copyLimit" min="0" value="<%= copyLimit %>">
        <br>
        <label for="reservation-limit-day">Limit:</label>
        <input type="number" id="reservation-limit-day" name="reservationLimitDay" min="0" value="<%= reservationLimitDay %>">
        <br>
        <button type="submit">Update Limits</button>
    </form>
</div>